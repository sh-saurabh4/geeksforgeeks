#include<bits/stdc++.h>
using namespace std;
int main(){
	string txt,pat;
	getline(cin,txt);
	getline(cin,pat);
	int n,l;
	n = txt.length();
	m = pat.length();
	int arr[m];
	int i,j=0,k=0;
	if(n == 0 || m == 0 || n < m )
		return 0;
	for(i = 0; i < n; i++){
		if(str[i] == str[j]){		
			j = 
